<UserControl x:Class="summary_list.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:summary_list.Views"
             xmlns:converters="clr-namespace:summary_list.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:CheckToColorConverter x:Key="CheckToColorConverter"/>
        <converters:WidthToColumnsConverter x:Key="WidthToColumnsConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="{Binding Title}" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Margin="10"
                   HorizontalAlignment="Center"/>
        
        <ItemsControl Grid.Row="1" 
                     ItemsSource="{Binding Items}"
                     Margin="10">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="{Binding RelativeSource={RelativeSource AncestorType=ItemsControl}, Path=ActualWidth, Converter={StaticResource WidthToColumnsConverter}}"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" 
                            BorderThickness="1" 
                            Margin="5" 
                            Padding="10"
                            Background="{Binding IsChecked, Converter={StaticResource CheckToColorConverter}}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding CheckSymbol}" 
                                     FontSize="20"
                                     FontWeight="Bold"
                                     Margin="0,0,5,0"
                                     Foreground="{Binding IsChecked, Converter={StaticResource CheckToColorConverter}}"/>
                            <TextBlock Text="{Binding Text}" 
                                     TextWrapping="Wrap"
                                     VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl> 